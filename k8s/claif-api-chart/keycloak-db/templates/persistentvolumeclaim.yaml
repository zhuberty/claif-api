{{- if .Values.keycloakDb.usePersistentStorage }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-keycloak-db-pvc
  namespace: {{ .Values.namespace }}
spec:
  storageClassName: {{ .Values.keycloakDb.persistentVolumeClaim.storageClassName }}
  accessModes:
    - {{ .Values.keycloakDb.persistentVolumeClaim.accessMode }}
  resources:
    requests:
      storage: {{ .Values.keycloakDb.persistentVolumeClaim.storage }}
  selector:
    matchLabels:
      pv-name: {{ .Release.Name }}-keycloak-db-pv
{{- end }}
